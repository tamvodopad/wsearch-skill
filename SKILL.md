---
name: wsearch
description: Search WW2 soldiers from warsearch.ru by location and export results to Excel. Use when user asks to find war participants, search soldiers by village/city, or collect WW2 data from warsearch.
allowed-tools: Bash,Read,Write,Grep,Glob,mcp__claude-in-chrome__*
---

# WSearch - Поиск участников ВОВ

Автоматизированный сбор данных об участниках Великой Отечественной войны с сайта warsearch.ru с экспортом в Excel.

## Выбор режима работы

**Сначала проверь доступность Chrome:**

1. Попробуй вызвать `mcp__claude-in-chrome__tabs_context_mcp`
2. Если Chrome доступен → используй браузерную автоматизацию (см. разделы ниже)
3. Если Chrome НЕ доступен → запусти Playwright скрипт:

```bash
# Установка зависимостей (если нужно)
pip3 install openpyxl playwright requests
python3 -m playwright install chromium

# Запуск (интерактивный режим — скрипт спросит параметры)
python3 wsearch.py

# Или с параметрами через командную строку:
python3 wsearch.py --location "Вишур" --district "Кизнерский" --variants "Вишур,Вышур,Вичур"
```

## КРИТИЧЕСКИ ВАЖНО: Особенности работы

### 1. Сайт ОЧЕНЬ медленный!

| Операция | Минимальное ожидание |
|----------|---------------------|
| Загрузка главной страницы | 10-15 сек |
| Загрузка результатов поиска | 15-30 сек |
| Загрузка карточки на pamyat-naroda.ru | 10-15 сек |
| Между запросами | 5 сек |

### 2. Одноимённые населённые пункты!

**ВАЖНО:** В России много сёл с одинаковыми названиями в разных районах!

Пример: "Вишур" есть в районах:
- Кизнерский (целевой)
- Шарканский
- Можгинский
- Нылгинский
- и других...

**Решение:** Всегда искать "Название + Район" (например: "Вишур Кизнерский")

## Когда использовать

- Пользователь просит найти участников ВОВ по населённому пункту
- Нужно собрать данные о солдатах для генеалогического исследования
- Требуется систематический поиск с разными вариантами написания

## Начальный сбор информации

Перед началом работы задай 5 вопросов:

### 1. Целевой населённый пункт
Точное название как в документах военного времени.

**ВАЖНО:** Всегда спрашивай у пользователя название населённого пункта. Не предлагай готовые варианты.

### 2. Административные привязки
Район, область или другие уточняющие маркеры.

### 3. Варианты написания
3-10 документированных вариантов (старые названия, ошибки транслитерации).

### 4. Критерии включения
- Место рождения (по умолчанию)
- Место жительства
- Место призыва
- Место захоронения

### 5. Целевая популяция
- Погибшие
- Пропавшие без вести
- Все участники (по умолчанию)

## Алгоритм работы (проверенный)

```
1. СБОР ПАРАМЕТРОВ
   └─ Задать 5 вопросов
   └─ Сформировать поисковый запрос: "Название + Район"

2. ПОИСК НА WARSEARCH.RU (браузер)
   ├─ Открыть https://warsearch.ru/
   ├─ ЖДАТЬ 15 сек
   ├─ Кликнуть вкладку "место рождения"
   ├─ ЖДАТЬ 3 сек
   ├─ Ввести запрос (например: "Вишур Кизнерский")
   ├─ Нажать "Искать"
   ├─ ЖДАТЬ 15-30 сек
   ├─ Выставить 500 элементов на страницу
   ├─ ЖДАТЬ загрузки
   └─ Парсить таблицу через JavaScript

3. ОБРАБОТКА ПАГИНАЦИИ
   ├─ Проверить количество страниц
   ├─ Для каждой страницы:
   │   ├─ Парсить таблицу
   │   ├─ Сохранять данные
   │   ├─ Переходить на следующую страницу
   │   └─ ЖДАТЬ 10-15 сек
   └─ Объединить все данные

4. ГЕОГРАФИЧЕСКАЯ ФИЛЬТРАЦИЯ
   ├─ Уровень A: Село + Район в месте рождения → Итог
   ├─ Уровень B: Село в рождении + Район в службе → Итог
   ├─ Уровень C: Только село без района → Кандидаты
   └─ Исключить записи из других районов!

5. ОБОГАЩЕНИЕ СТАТУСАМИ (Python скрипт)
   ├─ Для каждой записи из "Итог":
   │   ├─ Определить статус по типу URL (эвристика)
   │   ├─ Если неясно — загрузить страницу через requests
   │   └─ Извлечь статус из текста
   └─ Добавить цветовую маркировку

6. ЭКСПОРТ В EXCEL
   └─ Создать файл с 4 листами (см. ниже)
```

## Навигация по warsearch.ru

### Структура сайта

URL: `https://warsearch.ru/`

Сайт имеет вкладки:
- **Вкладка 1:** Поиск по фамилии
- **Вкладка 2:** Поиск по месту рождения ← ИСПОЛЬЗУЕМ ЭТУ
- **Вкладка 3:** Поиск по месту службы

### Элементы управления

- Поле ввода: `input[type='text']` или `input[placeholder*='место']`
- Кнопка поиска: текст "Искать"
- Переключатель страниц: 50/100/200/500 ← **ВЫСТАВИТЬ 500!**

### Результаты поиска

Таблица со столбцами:
| Фамилия | Имя | Отчество | Год рождения | Место рождения | Ссылка |

Ссылка ведёт на pamyat-naroda.ru для деталей.

## JavaScript для парсинга таблицы

```javascript
// Извлечение данных из таблицы warsearch.ru
var rows = document.querySelectorAll('table tbody tr');
var data = [];
rows.forEach(function(row) {
    var cells = row.querySelectorAll('td');
    if (cells.length >= 5) {
        var link = row.querySelector('a[href*="pamyat-naroda"]');
        data.push({
            f: cells[0].innerText.trim(),  // Фамилия
            n: cells[1].innerText.trim(),  // Имя
            p: cells[2].innerText.trim(),  // Отчество
            y: cells[3].innerText.trim(),  // Год
            b: cells[4].innerText.trim(),  // Место рождения
            s: '',                          // Место службы (заполнится позже)
            u: link ? link.href.split('?')[0] : ''  // URL без параметров
        });
    }
});
JSON.stringify(data);
```

## Географическая фильтрация

### Исключаемые районы (пример для Удмуртии)

При поиске по селу нужно исключать записи из других районов:

```javascript
var excludeDistricts = [
    'шарканск', 'можгинск', 'нылгинск', 'завьялов',
    'бемыжск', 'игринск', 'глазов', 'балезин',
    'воткинск', 'сарапул', 'увинск', 'якшур-бод'
];

function shouldExclude(birthPlace) {
    var place = birthPlace.toLowerCase();
    return excludeDistricts.some(d => place.includes(d));
}
```

### Логика верификации (A/B/C)

| Уровень | Критерий | Результат |
|---------|----------|-----------|
| **A** | Село + Район в месте рождения | → Итог (100%) |
| **B** | Село в рождении + Район в службе/призыве | → Итог (высокая) |
| **C** | Только село, район неизвестен | → Кандидаты |

## Обогащение статусами

### Эвристика по типу URL

Статус можно определить по URL карточки на pamyat-naroda.ru:

| Фрагмент URL | Статус |
|--------------|--------|
| `plen` | Плен |
| `vpp` | Пропал без вести |
| `donesenie` | Погиб/Пропал |
| `card_ran` | Ранен |
| `podvig` | Награждён (вероятно выжил) |
| `guk` | Неизвестен |

### Python скрипт для обогащения

```python
import requests

def extract_status_from_url(url):
    """Определяет статус по типу URL."""
    url_lower = url.lower()
    if 'plen' in url_lower:
        return 'Плен'
    if 'vpp' in url_lower:
        return 'Пропал без вести'
    if 'donesenie' in url_lower:
        return 'Погиб/Пропал'
    if 'card_ran' in url_lower:
        return 'Ранен'
    if 'podvig' in url_lower:
        return 'Награждён'
    return None

def extract_status_from_html(html):
    """Извлекает статус из HTML страницы."""
    text = html.lower()
    if 'погиб' in text or 'убит' in text:
        return 'Погиб'
    if 'пропал без вести' in text:
        return 'Пропал без вести'
    if 'умер от ран' in text:
        return 'Умер от ран'
    if 'умер в плену' in text:
        return 'Умер в плену'
    if 'военнопленн' in text:
        return 'Плен'
    if 'ранен' in text:
        return 'Ранен'
    if 'вернулся' in text or 'демобилизован' in text:
        return 'Вернулся'
    return 'Неизвестен'

def fetch_status(url):
    """Загружает страницу и извлекает статус."""
    # Сначала по URL
    status = extract_status_from_url(url)
    if status:
        return status

    # Загружаем страницу
    try:
        headers = {'User-Agent': 'Mozilla/5.0'}
        resp = requests.get(url, headers=headers, timeout=30)
        if resp.status_code == 200:
            return extract_status_from_html(resp.text)
    except:
        pass

    return 'Неизвестен'
```

**ВАЖНО:** Для массовой обработки (>10 записей) использовать Python/requests, а не браузерную автоматизацию! Браузер теряет контекст при навигации.

## Структура Excel (results.xlsx)

### ЧЕК-ЛИСТ: Обязательные требования к Excel

- [ ] **Лист "Итог"** — подтверждённые записи (уровень A/B)
- [ ] **Лист "Кандидаты"** — записи требующие проверки (уровень C)
- [ ] **Лист "Статистика"** — сводка по статусам
- [ ] **Лист "Варианты"** — использованные варианты написания
- [ ] **Колонка "Место рождения"** — **КАК ЕСТЬ** из таблицы warsearch.ru
- [ ] **Колонка "Место призыва/службы"** — **КАК ЕСТЬ** из таблицы warsearch.ru
- [ ] **Колонка "Статус"** — с ЦВЕТОВОЙ МАРКИРОВКОЙ
- [ ] **Колонка "Ссылка"** — КЛИКАБЕЛЬНАЯ (hyperlink), текст "Ссылка"
- [ ] **Заголовки** — синий фон, белый шрифт, жирный
- [ ] **Ширина колонок** — автоматическая подгонка

### ВАЖНО: Источники данных

| Колонка | Источник | Правило |
|---------|----------|---------|
| ФИО | warsearch.ru | Как есть (Фамилия + Имя + Отчество) |
| Год | warsearch.ru | Как есть |
| Место рождения | warsearch.ru | **КОПИРОВАТЬ КАК ЕСТЬ, не фантазировать!** |
| Место призыва/службы | warsearch.ru | **КОПИРОВАТЬ КАК ЕСТЬ, не фантазировать!** |
| Статус | pamyat-naroda.ru | Извлекается из карточки (этап обогащения) |
| Ссылка | warsearch.ru | URL на pamyat-naroda.ru из таблицы |

**НЕ МОДИФИЦИРОВАТЬ** данные из warsearch.ru! Брать значения напрямую из таблицы без изменений.

### Лист 1: "Итог" (подтверждённые записи)

| Колонка | Описание | Ширина |
|---------|----------|--------|
| № | Порядковый номер | 5 |
| ФИО | Фамилия Имя Отчество | 35 |
| Год | Год рождения | 8 |
| Место рождения | Полный адрес (область, район, село) | 45 |
| Место призыва/службы | РВК, воинская часть | 40 |
| Статус | С цветом: Погиб/Ранен/Пропал/и т.д. | 18 |
| Ссылка | **КЛИКАБЕЛЬНАЯ** — текст "Ссылка" | 10 |
| Уровень | A или B | 8 |

### Лист 2: "Кандидаты" (требуют проверки)

**ОБЯЗАТЕЛЬНО создавать!** Сюда идут записи уровня C (село без района).
Те же колонки что в "Итог", но уровень = C.

### Лист 3: "Статистика"

| Статус | Количество | Цвет ячейки |
|--------|------------|-------------|
| Пропал без вести | 14 | 🟡 жёлтый |
| Погиб | 4 | 🔴 красный |
| и т.д. | ... | ... |

### Лист 4: "Варианты"

Список всех использованных вариантов написания населённого пункта.

### Цветовая маркировка статусов (ОБЯЗАТЕЛЬНО!)

| Статус | HEX цвет | Визуально |
|--------|----------|-----------|
| Погиб | FF6B6B | 🔴 красный |
| Погиб/Пропал | FF6B6B | 🔴 красный |
| Умер от ран | FF8C8C | 🔴 светло-красный |
| Умер в плену | FF8C8C | 🔴 светло-красный |
| Пропал без вести | FFD93D | 🟡 жёлтый |
| Плен | FFA500 | 🟠 оранжевый |
| Ранен | 87CEEB | 🔵 голубой |
| Награждён | 90EE90 | 🟢 зелёный |
| Вернулся | 98FB98 | 🟢 светло-зелёный |
| Неизвестен | D3D3D3 | ⬜ серый |

## Создание Excel с кликабельными ссылками

```python
from openpyxl import Workbook
from openpyxl.styles import Font, PatternFill, Alignment, Border, Side

wb = Workbook()

# === ЛИСТ 1: ИТОГ ===
ws = wb.active
ws.title = "Итог"

# Заголовки (синий фон, белый шрифт)
headers = ["№", "ФИО", "Год", "Место рождения", "Место призыва/службы", "Статус", "Ссылка", "Уровень"]
ws.append(headers)

header_fill = PatternFill(start_color="4472C4", end_color="4472C4", fill_type="solid")
header_font = Font(bold=True, color="FFFFFF")
for col in range(1, len(headers) + 1):
    cell = ws.cell(row=1, column=col)
    cell.fill = header_fill
    cell.font = header_font
    cell.alignment = Alignment(horizontal='center')

# Цвета статусов
status_colors = {
    'Погиб': PatternFill(start_color="FF6B6B", fill_type="solid"),
    'Умер от ран': PatternFill(start_color="FF8C8C", fill_type="solid"),
    'Пропал без вести': PatternFill(start_color="FFD93D", fill_type="solid"),
    'Плен': PatternFill(start_color="FFA500", fill_type="solid"),
    'Ранен': PatternFill(start_color="87CEEB", fill_type="solid"),
    'Награждён': PatternFill(start_color="90EE90", fill_type="solid"),
    'Вернулся': PatternFill(start_color="98FB98", fill_type="solid"),
    'Неизвестен': PatternFill(start_color="D3D3D3", fill_type="solid"),
}

# Данные
for i, soldier in enumerate(data, 1):
    row_num = i + 1
    ws.append([i, soldier['fio'], soldier['year'], soldier['birth'],
               soldier['service'], soldier['status'], "", "A"])

    # Цвет статуса
    if soldier['status'] in status_colors:
        ws.cell(row=row_num, column=6).fill = status_colors[soldier['status']]

    # КЛИКАБЕЛЬНАЯ ССЫЛКА (обязательно!)
    link_cell = ws.cell(row=row_num, column=7)
    link_cell.hyperlink = soldier['url']
    link_cell.value = "Ссылка"
    link_cell.style = "Hyperlink"

# Ширина колонок
ws.column_dimensions['A'].width = 5   # №
ws.column_dimensions['B'].width = 35  # ФИО
ws.column_dimensions['C'].width = 8   # Год
ws.column_dimensions['D'].width = 45  # Место рождения
ws.column_dimensions['E'].width = 40  # Место призыва/службы
ws.column_dimensions['F'].width = 18  # Статус
ws.column_dimensions['G'].width = 10  # Ссылка
ws.column_dimensions['H'].width = 8   # Уровень

# === ЛИСТ 2: КАНДИДАТЫ (обязательно создавать!) ===
ws2 = wb.create_sheet("Кандидаты")
# ... те же колонки, заголовок оранжевый (ED7D31)

# === ЛИСТ 3: СТАТИСТИКА ===
ws3 = wb.create_sheet("Статистика")

# === ЛИСТ 4: ВАРИАНТЫ ===
ws4 = wb.create_sheet("Варианты")

wb.save("results.xlsx")
```

## Нормализация статусов

| Исходный текст | Нормализованный |
|----------------|-----------------|
| погиб, убит | Погиб |
| пропал без вести, пропал б/в | Пропал без вести |
| умер от ран | Умер от ран |
| умер в плену | Умер в плену |
| военнопленный | Плен |
| ранен | Ранен |
| награждён | Награждён |
| вернулся, жив, демобилизован | Вернулся |

## Обработка ошибок

### Страница не загружается
1. Подождать 30 секунд
2. Обновить страницу
3. Если не помогло — записать в лог и перейти к следующему

### Браузер теряет контекст
При навигации window.* переменные сбрасываются.
**Решение:** Сохранять данные в JSON файл после каждого этапа.

### Капча или блокировка
Сообщить пользователю, приостановить работу.

## Ограничения

- **Сайт очень медленный** — соблюдай таймауты!
- Для >10 записей используй Python/requests вместо браузера
- Статусы определяются эвристически, могут быть неточности
- Одноимённые сёла требуют фильтрации по району
